r = [ 42488261.00028026, -50635537.69016445, 0];
v = [-754.37804831, 696.12410414, 0];

vector_to_elements(r, v, mu)


function [a e i lan w ta] = vector_to_elements(rvec, vvec)
	r = norm(rvec);
	v = norm(vvec); 
	
	hvec = cross(rvec, vvec); 
	h = norm(hvec)
end